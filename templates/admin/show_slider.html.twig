{% extends 'base.html.twig' %}

{% block title %}Gestion des Sliders{% endblock %}

{% block body %}


 <div class="row mx-auto">
        <div class="col-4"></div>
        <div class="col-4"><a href="{{ path('create_slider') }}" class="btn btn-primary">Ajouter un slider</a></div>
        
    </div>
    <h1 class="text-center my-4">Slider</h1>

    <div class="row">
        <div class="col-6 mx-auto">
            {% include 'include/_flashes.html.twig' %}
        </div>
    </div>

    <div class="row mx-auto">
        <div class="col-4"></div>
        <div class="col-4"><a href="#" class="btn btn-warning"><i class="fa fa-trash"></i> Voir les archives</a> </div>
    </div>

    <div class="row">
        <div class="col-10 mx-auto my-4">

            <h2 class="text-center">Slider</h2>

            <table class="table">
                <thead>
                <tr>
                    <th>#</th>
                    <th>Photo</th>
                    <th>Ordre</th>
                    <th>Crée le </th>
                    <th>Modifié le </th> 
                    <th>Actions</th>   
                </tr>
                </thead>
                <tbody>
                
                {% for slider in sliders %} 
                <tr>
                 <td>{{ slider.id }}</td>
                 <td>{{ slider.photo }}</td>
                 <td>{{ slider.ordre }}</td>
                 <td>{{ slider.createdAt | date('d/m/Y')}}</td>
                 <td>{{ slider.updatedAt | date('d/m/Y')}}</td>
                  <td>
                            <a href="#" class="text-info" title="Modifier le slider"><i class="fa fa-edit"></i></a>
                            <a href="#" class="text-danger" title="Archiver le slider" onclick="return confirm('Voulez-vous archiver ce slider ?')"><i class="fa fa-trash"></i></a>
                        </td>
                 
                </tr>

                {% endfor %}
      
 {% endblock %}