{% extends 'base.html.twig' %}

{% block title %}Gestion des commandes{% endblock %}

{% block body %}

    <div class="row">
        <div class="col-6 mx-auto">
            {% include 'include/_flashes.html.twig' %}
        </div>
    </div>

   {#<div class="row mx-auto">
        <div class="col-4"></div>
        <div class="col-4"><a href="{{ path('create_commande') }}" class="btn btn-primary">Voir les commandes</a> </div>
    </div>#} 

    <div class="row">
        <div class="col-10 mx-auto my-4">

            <h2 class="text-center">Commandes</h2>

            <table class="table">
                <thead>
                <tr>
                    <th>Commande</th>
                    <th>Chambres</th>
                    <th>DateArrivée</th>
                    <th>DateDepart</th>
                    <th>PrixTotal</th>
                    <th>Prenom</th> 
                    <th>Nom</th>   
                    <th>Télephone</th>   
                    <th>Email</th>   
                    <th>dateEnregistrement</th>   
                    <th>Actions</th>   
                </tr>
                </thead>
                <tbody>
                
                {% for commande in commandes %} 
                <tr>
                 <td>{{ commande.id }}</td>
                 <td>{{ chambre.id }}</td>  
                 <td>{{ commande.dateArrivee }}</td>
                 <td>{{ commnde.dateDepart }}</td>
                 <td>{{ commande.prixTotal }}</td>
                 <td>{{ commande.prenom }}</td>
                 <td>{{ commande.nom }}</td>
                 <td>{{ commande.telephone }}</td>
                 <td>{{ commande.email }}</td>
                 <td>{{ commande.createdAt | date('d/m/Y')}}</td>
                 <td>{{ commande.updatedAt | date('d/m/Y')}}</td>
                  <td>
                            <a href="#" class="text-info" title="Rechercher une commande"><i class="fa fa-edit"></i></a>
                            <a href="#" class="text-info" title="Modifier une commande"><i class="fa fa-edit"></i></a>
                            <a href="#" class="text-danger" title="Supprimer une commande" onclick="return confirm('Voulez-vous supprimer cette commande ?')"><i class="fa fa-trash"></i></a>
                        </td>
                 
                </tr>
                {% endfor %}


                
                 <div class="row">
          <div class="col-6 mx-auto">
          
         
          <div class="row">
             <div class="col"></div>
           
          </div>
          

          </div>
     </div>
     {% endblock %}